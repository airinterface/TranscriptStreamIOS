# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'
workspace 'TranscriptStream'

target 'TranscriptStream' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TranscriptStream
  pod 'AWSMobileClient'
  pod 'AWSPinpoint'
  pod 'AWSTranscribeStreaming'
  pod 'AWSRuntime'
  
  target 'TranscriptStreamTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TranscriptStreamUITests' do
    # Pods for testing
  end

end
